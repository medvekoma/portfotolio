@model ListItems
@using Portfotolio.Domain
@using Portfotolio.Site4.Extensions

<ul id="groups">
@foreach (var item in Model.Items)
{
    <li>
        <a href='@Url.ActionUserId("group", new {id = item.Id})' title='@item.Name'>@item.Name</a>
    </li>
}
</ul>